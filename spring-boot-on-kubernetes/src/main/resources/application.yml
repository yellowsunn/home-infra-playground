spring:
  application:
    name: spring-boot-example
  graphql:
    graphiql:
      enabled: true
    schema:
      printer:
        enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
